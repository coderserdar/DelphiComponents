unit info;

interface

uses
  Windows, Messages, SysUtils, Classes, Graphics, Controls, Forms, Dialogs,
  StdCtrls, ExtCtrls, XPStyler;

type
  TXPInfoDialog = class(TForm)
    Image1: TImage;
    LogoImage: TImage;
    Bevel1: TBevel;
    Button1: TButton;
    Label1: TLabel;
    Label2: TLabel;
    AppName: TLabel;
    AppVersion: TLabel;
    AppCopyright: TLabel;
    AppLicense: TLabel;
    XPStyler1: TXPStyler;
  private
    { Private-Deklarationen }
  public
    { Public-Deklarationen }
  end;

var
  XPInfoDialog: TXPInfoDialog;

implementation

{$R *.DFM}

end.
