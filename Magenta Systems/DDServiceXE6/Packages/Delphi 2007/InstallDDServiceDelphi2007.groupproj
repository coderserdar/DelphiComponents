<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <ProjectGuid>{2ec2c157-c9e5-4b8e-9210-b9a58b3c89ca}</ProjectGuid>
  </PropertyGroup>
  <ItemGroup />
  <ItemGroup>
    <Projects Include="DDServiceDesign.dproj" />
    <Projects Include="DDServiceRun.dproj" />
  </ItemGroup>
  <ProjectExtensions>
    <Borland.Personality>Default.Personality</Borland.Personality>
    <Borland.ProjectType />
    <BorlandProject>
<BorlandProject xmlns=""><Default.Personality></Default.Personality></BorlandProject></BorlandProject>
  </ProjectExtensions>
  <Target Name="DDServiceRun">
    <MSBuild Projects="DDServiceRun.dproj" Targets="" />
  </Target>
  <Target Name="DDServiceRun:Clean">
    <MSBuild Projects="DDServiceRun.dproj" Targets="Clean" />
  </Target>
  <Target Name="DDServiceRun:Make">
    <MSBuild Projects="DDServiceRun.dproj" Targets="Make" />
  </Target>
  <Target Name="DDServiceDesign">
    <MSBuild Projects="DDServiceDesign.dproj" Targets="" />
  </Target>
  <Target Name="DDServiceDesign:Clean">
    <MSBuild Projects="DDServiceDesign.dproj" Targets="Clean" />
  </Target>
  <Target Name="DDServiceDesign:Make">
    <MSBuild Projects="DDServiceDesign.dproj" Targets="Make" />
  </Target>
  <Target Name="Build">
    <CallTarget Targets="DDServiceRun;DDServiceDesign" />
  </Target>
  <Target Name="Clean">
    <CallTarget Targets="DDServiceRun:Clean;DDServiceDesign:Clean" />
  </Target>
  <Target Name="Make">
    <CallTarget Targets="DDServiceRun:Make;DDServiceDesign:Make" />
  </Target>
  <Import Condition="Exists('$(MSBuildBinPath)\Borland.Group.Targets')" Project="$(MSBuildBinPath)\Borland.Group.Targets" />
</Project>