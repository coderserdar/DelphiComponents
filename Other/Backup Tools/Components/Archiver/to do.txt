  TO DO:
  ------
  * Add a mode to store diffs of files in order to make patches.
  * Add an Update/Freshen feature
  * Use the Archive bit in order to select files that need to be backed up.
  * Add Find file in WinArchiver
  * Use Win95 style components that display a folder content
    (instead of TDirectoryListbox...).
  * Add Shell extensions
  * Make a SelectDirectory function that would let us translate the
    string messages. --> use the BrowseFolder API instead !
  * Add a Join of segments
  * Add a Split archive in segments

